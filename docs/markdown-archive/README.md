# Markdown archive

Legacy implementation notes, UI patterns, and one-off guides moved from the old `markdown files/` folder. Prefer current sources:

- **User-facing help:** `WebPortal/help/index.html`, `WebPortal/help/user-manual.html`, `WebPortal/assets/docs/user-guide.html`
- **Cursor rules:** `.cursor/rules/`
- **Migrations:** `migrations/`

Do not commit generated MCP/batch segments (`agent-tools/`, `scripts/_soh_batches/` — see repo `.gitignore`).
