# IDE and editor files
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Debug logs
debug.log

# Node modules (if added later)
node_modules/

# Environment files
.env
.env.local

# Build artifacts
dist/
build/
*.log


# Supabase CLI machine-local state (link target, cached versions)
supabase/.temp/

# Agent config: agents/ and rules/ are shared and MUST be tracked (the
# model-delegation standard lives there). Only per-developer settings and
# fleet plan scratch stay out of git.
.claude/settings.local.json
.cursor/plans/