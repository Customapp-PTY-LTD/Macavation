# Modal: Production Stages (View)

Read-only view of production stages.

## Instructions / Source

- **Behaviour**: `modules/kernel-production/js/kernel_production_stages.js` (e.g. `showProductionStagesViewModal()`).
- Container ID: `#productionStagesViewModal`. Route name: `production-stages-view-modal`.
