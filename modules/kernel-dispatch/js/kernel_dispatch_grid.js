/**
 * Kernel Dispatch - INV from KERNEL R YES → KERNEL CUSTOMERS → DEBTORS.
 */
(function () {
    if (typeof window.initializeKernelDispatchGrid === 'function') return;
    window.initializeKernelDispatchGrid = function () { /* placeholder */ };
})();
