/**
 * Oil & Protein Dispatch - INV from OIL PROTEIN R YES → FEED+OIL+PROTEIN CUSTOMERS → DEBTORS.
 */
(function () {
    if (typeof window.initializeOilDispatchGrid === 'function') return;
    window.initializeOilDispatchGrid = function () { /* placeholder */ };
})();
