# Record a confirmed repo fact in BluePrint/FLEET_LESSONS.md

A fleet run (fde52028-d6d7-4c3c-acbe-eba597b7ee95) was blocked, and the root cause was traced to a fact about this repo's own
current code that is worth recording so a future plan-review run is pointed at it directly instead
of re-discovering it the hard way. This plan does exactly one thing: append that fact as a single
Markdown bullet to `BluePrint/FLEET_LESSONS.md`, creating the file (with a one-line header) if it
does not already exist.

## What to append

- `WebPortal/js/macavation-supabase.js` is generated by `scripts/sync-portal-supabase-config.mjs` (`npm run supabase:sync-portal`) and deliberately hardcodes the prod/dev Supabase URLs and anon keys that `scripts/check-supabase-project.mjs` requires and guards; the portal loads it as a plain `<script>` (e.g. `WebPortal/signin.html:196`) with no bundler, so `process.env` is unavailable in portal browser code and exists only in the server-side handler `WebPortal/index_supabase.js`.

## Steps

1. If `BluePrint/FLEET_LESSONS.md` does not exist, create it with this header:
   ```markdown
   # Fleet lessons - confirmed facts about this repo's own code

   Each entry below was confirmed against this repo's actual code after a fleet run was blocked.
   plan-review.ts points future plan reviews at these as a strong starting pointer, never as
   authoritative on its own - re-verify against the current code before treating a contradiction
   as a defect, and prune an entry the current code no longer supports.
   ```
2. Append the bullet above as a new line at the end of the file's list of entries (start the list
   if the file was just created).
3. Make no other change. Do not touch any other file.

## Out of scope

- Rewriting, reordering, or pruning any existing entry.
- Anything about this repo's code other than the one line above.

## Verify before finishing

- `BluePrint/FLEET_LESSONS.md` exists and its last entry is exactly the bullet above, with every
  entry that existed before still present and unchanged.
